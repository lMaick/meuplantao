"use client";
import { FormEvent, useState } from "react";

const SUPPORT_EMAIL = "suporte@meuplantao.app";
export function SupportForm() { const [sent, setSent] = useState(false); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const subject = String(form.get("subject") || "Feedback sobre o MeuPlantao"); const message = String(form.get("message") || ""); window.location.href = `mailto:${SUPPORT_EMAIL}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(message)}`; setSent(true); } return <form onSubmit={submit} className="space-y-4 rounded-lg border p-4"><div><label htmlFor="support-subject" className="font-medium">Assunto</label><input id="support-subject" name="subject" required className="mt-1 h-11 w-full rounded-md border bg-background px-3" /></div><div><label htmlFor="support-message" className="font-medium">Mensagem</label><textarea id="support-message" name="message" required rows={5} className="mt-1 w-full rounded-md border bg-background p-3" /></div><button type="submit" className="min-h-11 rounded-md bg-primary px-4 font-medium text-primary-foreground">Abrir e-mail</button>{sent && <p role="status" className="text-sm text-muted-foreground">Seu aplicativo de e-mail deve abrir com a mensagem preparada.</p>}</form>; }
